import type { NextConfig } from "next";

// Default Next.js config — no project-specific overrides are needed yet.
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
